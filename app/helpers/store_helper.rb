module StoreHelper

# include StoreIndexCount

#  def index_count
#    if session[:counter] = 1
#      index_count = 'Visit ' + session[:counter].to_s + ' Time'
#    elsif session[:counter] > 1 and session[:counter] <= 5
#      index_count = 'Visit ' + session[:counter].to_s + ' Times'
#    else
#      index_count = session[:counter].to_s
#   end
#  end

end
